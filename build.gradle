plugins {
    id 'java'
}

group = 'com.example'
version = '1.0'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.2'
}

test {
    useJUnitPlatform()
}

tasks.register('runServer5', JavaExec) {
    group = "run"
    description = "Run server with periodic verification = 5s"
    classpath = sourceSets.main.runtimeClasspath
    mainClass = "com.example.server.ServerMain"
    systemProperties = [
            "port"       : "5050",
            "p"          : "10",
            "verifySec"  : "5",
            "runSeconds" : "180",
            "tPlataSec"  : "20",
            "outDir"     : "output/5s"
    ]
}

tasks.register('runServer10', JavaExec) {
    group = "run"
    description = "Run server with periodic verification = 10s"
    classpath = sourceSets.main.runtimeClasspath
    mainClass = "com.example.server.ServerMain"
    systemProperties = [
            "port"       : "5050",
            "p"          : "10",
            "verifySec"  : "10",
            "runSeconds" : "180",
            "tPlataSec"  : "20",
            "outDir"     : "output/10s"
    ]
}

tasks.register('runClients5', JavaExec) {
    group = "run"
    description = "Run clients for 5s audit run"
    classpath = sourceSets.main.runtimeClasspath
    mainClass = "com.example.client.ClientMain"
    systemProperties = [
            "host"    : "127.0.0.1",
            "port"    : "5050",
            "clients" : "10",
            "tag"     : "5s"
    ]
}

tasks.register('runClients10', JavaExec) {
    group = "run"
    description = "Run clients for 10s audit run"
    classpath = sourceSets.main.runtimeClasspath
    mainClass = "com.example.client.ClientMain"
    systemProperties = [
            "host"    : "127.0.0.1",
            "port"    : "5050",
            "clients" : "10",
            "tag"     : "10s"
    ]
}

